# docker compose -f docker-compose.test.yml up --build --abort-on-container-exit
services:
  backend-tests:
    build:
      context: ./backend
      target: test
  frontend-tests:
    build:
      context: ./frontend
      target: test
